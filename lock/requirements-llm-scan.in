# Input for lock/requirements-llm-scan.txt (python -m src.deps_lock --compile):
# the LLM security scan installs the compiled, hash-pinned file with
# --require-hashes. Bump here, then --compile; Dependabot proposes bumps
# for the compiled file.
openai==3.19.0
anthropic==1.8.0
