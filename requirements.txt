# Curatarr — Requirements
# Pinned 2026-08-20 to the versions of the live, fully-tested install
# (48/48 test suites green). Bump deliberately, then re-run
# `python tests/run_all.py`.

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# Database
sqlalchemy==2.0.52

# Auth & security
python-jose[cryptography]==3.5.0

# HTTP client
httpx==0.28.1
aiohttp==3.14.3

# System / process monitoring
psutil==7.2.2

# Vector store
chromadb==1.5.9

# Settings
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3

# Data
numpy==2.5.2

# Logging
python-json-logger==4.2.0

# Scheduler
apscheduler==3.11.3

# Tray launcher (curatarr_tray.pyw — background app, no console)
pystray==0.19.5
pillow==12.3.0
