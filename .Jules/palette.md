## 2024-05-30 - Focus and ARIA states in basic HTML/CSS
**Learning:** Found that this app mostly uses plain HTML and embedded CSS for its frontend (index.html). Accessibility enhancements like `:focus-visible` and `aria-label` require careful regex or manual finding since there are no generic UI components abstractions like in React.
**Action:** Always search carefully for all instances when applying global a11y improvements to ensure all elements are captured.
